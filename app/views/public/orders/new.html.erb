<h1>注文情報入力</h1>

<h2>支払い方法</h2>

<%= form_with model: @new_order, public_orders_path, method: :post, local:true do |f| %>
<p><%= f.radio_button :is_active, "販売中" %>販売中</p>
<p><%= f.radio_button :is_active, "販売停止中" %>販売停止中</p>
<% end %>

<h2>お届け先</h2>